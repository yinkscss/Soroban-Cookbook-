[package]
name = "events"
version = "0.1.0"
edition = "2021"

[lib]
name = "events_structured"
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
